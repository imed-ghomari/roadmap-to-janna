views:
  - type: table
    name: Table
    filters:
      and:
        - type == "process"
        - recurrence.isEmpty()
        - file.path.contains("Processes")
        - status == "working"
    order:
      - status
      - detail
      - file.name
    sort:
      - property: detail
        direction: ASC
