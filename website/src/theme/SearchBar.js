import React from 'react';

const SearchBar = () => {
  return (
    <div>
      <p></p>
      {/* Alternatively, if you want to keep the input but disable it, use this: */}
      {/* 
      <input
        type="text"
        placeholder="Search is disabled"
        disabled
      />
      */}
    </div>
  );
};

export default SearchBar;
